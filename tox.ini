[tox]
envlist = py27, py37

[testenv]
deps =
     pytest
     pytest-cov

install_command =
     python -m pip install {opts} --no-cache-dir {packages}

commands =
     pytest --cov caliper --basetemp={envtmpdir} {posargs} tests/
